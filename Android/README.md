# Android target notes

Android is an architectural target, not a currently supported native backend. The first milestone is cross-compiling the Ric frontend/runtime with the Android NDK after platform-neutral runtime interfaces are stable.

No Android support badge should be added until CI builds and executes an Android-target smoke test.
