# Benchmarks

Benchmarks are performance probes, not correctness tests. They should remain valid `.nqr` programs and must not introduce syntax that the current compiler does not implement.

`arithmetic.nqr` is a small interpreter/compiler pipeline seed. Future benchmark work should record compiler version, target, build type, machine/OS and wall-clock methodology so results remain comparable.
