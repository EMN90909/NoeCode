# Editor setup

VS Code-compatible editors can use the package in `editors/vscode/`. JetBrains IDEs can import the TextMate grammar documented in `editors/jetbrains/`. Vim/Neovim and Sublime definitions are also checked in. Associate the language with `*.nqr` and configure the language server command as `noqeri lsp` when the editor supports a generic LSP client.
