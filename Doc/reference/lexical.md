# Lexical reference

`.nqr` files are UTF-8 text. The current lexer recognizes identifiers, integer/float/string literals, line comments, delimiters, arithmetic/comparison/logical operators and the reserved words documented in `LANGUAGE_REFERENCE.md`. New keywords require lexer, parser, formatter, editor grammar and test changes together.
