# Command-line usage

Use `noqeri --version` to inspect compiler/language/target versions. `check` is the fastest validation path; `run` executes optimized NIR; `build` invokes the current native backend; `format` rewrites a source file; `test` recursively executes `.nqr` test programs; `doctor` validates repository release structure; `lsp` speaks JSON-RPC over stdio.
