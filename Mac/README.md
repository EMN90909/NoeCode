# macOS platform integration

The compiler frontend, NIR interpreter, formatter, test runner and LSP bootstrap build on macOS through CMake/Clang. Native Mach-O emission is planned and is not reported as supported.
