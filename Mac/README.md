# macOS platform integration

Ric's compiler frontend, NIR interpreter, formatter, test runner and LSP bootstrap build on macOS through CMake/Clang.

Native Mach-O emission is a planned backend and is not reported as supported. Apple-specific runtime/linker work belongs here once implemented.
