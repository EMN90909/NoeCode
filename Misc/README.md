# Miscellaneous project records

This directory is for release notes, acknowledgements and project records that do not belong to the language reference or compiler implementation. User-facing change history remains canonical in the root `CHANGELOG.md`.
