# Windows platform integration

The C++17 frontend/interpreter is supported on Windows through CMake and an MSVC-compatible toolchain. Native PE/COFF emission is not yet implemented. Windows runtime, console, filesystem and process integration belongs here; build-generator details belong in `PCbuild/`.
