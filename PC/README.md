# Windows platform integration

Ric's C++17 frontend/interpreter is supported on Windows through CMake and MSVC-compatible builds. The native PE/COFF backend is not yet implemented.

Windows-specific runtime, console, filesystem and process integration belongs here; build-generator details belong in `PCbuild/`.
