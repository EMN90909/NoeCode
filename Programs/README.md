# Programs

The current executable entry point is `compiler/bootstrap/main.cpp` and builds as `ric`.

This directory owns command-line program contracts, future auxiliary executables and install layout. User-facing command names must remain consistent with `README.md` and `Doc/TOOLCHAIN.md`.
