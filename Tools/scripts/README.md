# Repository scripts

Executable build/test/release scripts are kept in the root `scripts/` directory for discoverability. This directory documents ownership: scripts must be non-interactive in CI, fail on errors, avoid hidden global state and keep Unix/PowerShell behavior aligned.
