extern function tcpHostServe(address: string, handler: string): isize

function echo_handler(): int { return 0 }

function main(): int {
    unsafe {
        let server: isize = tcpHostServe("127.0.0.1:9090", "echo_handler")
        if server < 0 as isize { return 1 }
    }
    return 0
}
