import "../../../Lib/std/http.nqr"

function health(): int { return 200 }

function main(): int {
    let server: isize = serve("127.0.0.1:8080", "health")
    if server < 0 as isize { return 1 }
    return 0
}
