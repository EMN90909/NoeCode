# Programs

This directory is reserved for standalone programs that ship with or are built from the Noe toolchain.

The current command-line driver is implemented in `compiler/bootstrap/main.cpp`. Future versions may split the CLI, package manager, language server and formatter into separate executables or self-hosted Noe programs.
