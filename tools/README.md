# Tools

This directory is for Noe developer tools that should live beside the compiler rather than inside examples or docs.

Planned tools:

- syntax grammar generators
- release packaging scripts
- installer helpers
- benchmark runners
- LSP/editor integration utilities
- package registry tools

The current bootstrap keeps active build/test scripts in `scripts/`.
